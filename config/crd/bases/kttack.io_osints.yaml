---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: osints.kttack.io
spec:
  group: kttack.io
  names:
    kind: Osint
    listKind: OsintList
    plural: osints
    singular: osint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Osint is the Schema for the osints API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OsintSpec defines the desired state of Osint
            properties:
              additionalEnv:
                description: AdditionalEnv specifies additional environment variables
                items:
                  description: EnvVar represents an environment variable
                  properties:
                    name:
                      description: Name of the environment variable
                      type: string
                    value:
                      description: Value of the environment variable
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              args:
                description: Args specifies additional command-line arguments
                items:
                  type: string
                type: array
              assetPool:
                description: AssetPool references an AssetPool resource for OSINT
                  targets
                type: string
              capabilities:
                description: Capabilities specifies additional Linux capabilities
                items:
                  type: string
                type: array
              category:
                description: Category classifies the type of OSINT operation
                type: string
              debug:
                description: Debug enables debug mode
                type: boolean
              fileName:
                description: FileName specifies the output file name
                type: string
              httpProxy:
                description: HTTPProxy specifies an HTTP proxy to use
                type: string
              periodic:
                description: Periodic indicates if this should run as a CronJob
                type: boolean
              port:
                description: Port specifies a port (if applicable)
                type: string
              schedule:
                description: Schedule defines the cron schedule (required if Periodic
                  is true)
                type: string
              storagePool:
                description: StoragePool references a StoragePool resource for file
                  inputs
                type: string
              target:
                description: Target specifies the target username(s) for OSINT operations
                type: string
              targetPool:
                description: TargetPool references a TargetPool resource
                type: string
              tool:
                description: Tool specifies which OSINT tool to use (e.g., sherlock)
                type: string
            required:
            - tool
            type: object
          status:
            description: OsintStatus defines the observed state of Osint
            properties:
              jobName:
                description: JobName is the name of the created Job or CronJob
                type: string
              lastExecuted:
                description: LastExecuted timestamp of the last execution
                type: string
              resolvedAsset:
                description: ResolvedAsset is the actual asset value after AssetPool
                  resolution
                type: string
              resolvedAssetType:
                description: ResolvedAssetType is the type of asset after AssetPool
                  resolution
                type: string
              resolvedPort:
                description: ResolvedPort is the actual port after TargetPool resolution
                type: string
              resolvedTarget:
                description: ResolvedTarget is the actual target after TargetPool
                  resolution
                type: string
              resultsLocation:
                description: ResultsLocation indicates where results are stored
                type: string
              state:
                description: State represents the current state of the attack
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
