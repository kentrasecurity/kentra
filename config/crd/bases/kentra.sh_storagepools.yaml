---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: storagepools.kentra.sh
spec:
  group: kentra.sh
  names:
    kind: StoragePool
    listKind: StoragePoolList
    plural: storagepools
    shortNames:
    - sp
    singular: storagepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.files
      name: Files
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .status.lastSyncTime
      name: LastSync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StoragePool is the Schema for the storagepools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StoragePoolSpec defines the desired state of StoragePool
            properties:
              description:
                description: Description is a human-readable description of this storage
                  group
                type: string
              files:
                description: Files is a list of files to download from S3 bucket (s3://configs)
                items:
                  type: string
                type: array
            type: object
          status:
            description: StoragePoolStatus defines the observed state of StoragePool
            properties:
              fileCount:
                description: FileCount is the number of files in this storage group
                type: integer
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed StoragePool
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
