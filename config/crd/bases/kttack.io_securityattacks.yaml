---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: securityattacks.kttack.io
spec:
  group: kttack.io
  names:
    kind: SecurityAttack
    listKind: SecurityAttackList
    plural: securityattacks
    shortNames:
    - sa
    singular: securityattack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.attackType
      name: Type
      type: string
    - jsonPath: .spec.target
      name: Target
      type: string
    - jsonPath: .spec.tool
      name: Tool
      type: string
    - jsonPath: .spec.periodic
      name: Periodic
      type: boolean
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecurityAttack is the Schema for the securityattacks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityAttackSpec defines the desired state of SecurityAttack
            properties:
              additional_env:
                description: AdditionalEnv contains additional environment variables
                items:
                  description: EnvVar represents an environment variable
                  properties:
                    name:
                      description: Name of the environment variable
                      type: string
                    value:
                      description: Value of the environment variable
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              args:
                description: Args contains additional arguments for the tool
                items:
                  type: string
                type: array
              attackType:
                description: AttackType specifies the type of security test
                enum:
                - Enumeration
                - Vulnerability
                - Exploitation
                type: string
              debug:
                description: Debug enables debug mode (output to stdout instead of
                  log file)
                type: boolean
              http_proxy:
                description: HTTPProxy is the HTTP proxy URL to use
                type: string
              periodic:
                description: Periodic indicates if this should run on a schedule
                type: boolean
              schedule:
                description: Schedule is the cron schedule (only used if Periodic
                  is true)
                type: string
              target:
                description: Target is the IP, CIDR, or hostname to attack
                type: string
              tool:
                description: Tool is the security tool to use
                type: string
            required:
            - attackType
            - target
            - tool
            type: object
          status:
            description: SecurityAttackStatus defines the observed state of SecurityAttack
            properties:
              jobName:
                description: JobName is the name of the created job or cronjob
                type: string
              lastExecuted:
                description: LastExecuted is the timestamp of last execution
                type: string
              state:
                description: State represents the current state
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
