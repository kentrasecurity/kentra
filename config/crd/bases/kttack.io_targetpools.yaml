---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: targetpools.kttack.io
spec:
  group: kttack.io
  names:
    kind: TargetPool
    listKind: TargetPoolList
    plural: targetpools
    shortNames:
    - tp
    singular: targetpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target
      name: Target
      type: string
    - jsonPath: .spec.port
      name: Port
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TargetPool is the Schema for the targetpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetPoolSpec defines the desired state of TargetPool
            properties:
              description:
                description: Description is an optional description of the target
                  group
                type: string
              port:
                description: Port is the port or port range to target (e.g., '22',
                  '80,443', '8000-8100')
                type: string
              target:
                description: Target is the IP address, CIDR, or hostname to target
                type: string
            required:
            - target
            type: object
          status:
            description: TargetPoolStatus defines the observed state of TargetPool
            properties:
              lastUpdated:
                description: LastUpdated is the timestamp of last update
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
