---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: targetpools.kentra.sh
spec:
  group: kentra.sh
  names:
    kind: TargetPool
    listKind: TargetPoolList
    plural: targetpools
    singular: targetpool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TargetPool is the Schema for the targetpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetPoolSpec defines the desired state of TargetPool
            properties:
              description:
                type: string
              targets:
                items:
                  description: TargetSpec defines a named target with endpoints and
                    ports
                  properties:
                    endpoint:
                      description: Endpoint can be a single value or array of IPs,
                        CIDRs, or domains
                      items:
                        type: string
                      minItems: 1
                      type: array
                    name:
                      description: Name is the identifier for this target
                      type: string
                    port:
                      description: Port can be a single value or array of ports or
                        port ranges (e.g., "22", "80", "22-443")
                      items:
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - endpoint
                  - name
                  - port
                  type: object
                type: array
            required:
            - targets
            type: object
          status:
            description: TargetPoolStatus defines the observed state of TargetPool
            properties:
              lastUpdated:
                type: string
              observedGeneration:
                format: int64
                type: integer
              totalTargets:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
