---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: assetpools.kttack.io
spec:
  group: kttack.io
  names:
    kind: AssetPool
    listKind: AssetPoolList
    plural: assetpools
    shortNames:
    - ap
    singular: assetpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.itemCount
      name: Items
      type: integer
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AssetPool is the Schema for the assetpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AssetPoolSpec defines the desired state of AssetPool
            properties:
              description:
                description: Description is an optional description of the asset pool
                type: string
              items:
                description: Items is the list of assets in this pool
                items:
                  description: AssetItem defines a single asset in the pool
                  properties:
                    type:
                      description: Type specifies the type of asset (e.g., username,
                        email, domain, ip)
                      type: string
                    value:
                      description: Value is the actual value of the asset
                      type: string
                  required:
                  - type
                  - value
                  type: object
                type: array
            required:
            - items
            type: object
          status:
            description: AssetPoolStatus defines the observed state of AssetPool
            properties:
              itemCount:
                description: ItemCount is the number of items in this asset pool
                type: integer
              lastUpdated:
                description: LastUpdated is the timestamp of last update
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
