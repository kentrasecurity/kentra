---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: assetpools.kentra.sh
spec:
  group: kentra.sh
  names:
    kind: AssetPool
    listKind: AssetPoolList
    plural: assetpools
    shortNames:
    - ap
    singular: assetpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.itemCount
      name: Items
      type: integer
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AssetPool is the Schema for the assetpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AssetPoolSpec defines the desired state of AssetPool
            properties:
              description:
                type: string
              group:
                items:
                  description: |-
                    AssetGroup represents a logical grouping of multiple asset sets
                    For example: a person with their primary and secondary contact info
                  properties:
                    assets:
                      items:
                        description: AssetItem defines a single asset in the pool
                        properties:
                          type:
                            description: 'Type es: "username", "email", "phone"'
                            type: string
                          value:
                            description: Value deve essere una stringa singola
                            type: string
                        required:
                        - type
                        - value
                        type: object
                      type: array
                    name:
                      type: string
                  type: object
                type: array
              items:
                description: Manteniamo Items per compatibilità se lo usi ancora all'esterno
                  dei gruppi
                items:
                  description: AssetItem defines a single asset in the pool
                  properties:
                    type:
                      description: 'Type es: "username", "email", "phone"'
                      type: string
                    value:
                      description: Value deve essere una stringa singola
                      type: string
                  required:
                  - type
                  - value
                  type: object
                type: array
            type: object
          status:
            description: AssetPoolStatus defines the observed state of AssetPool
            properties:
              groupCount:
                type: integer
              itemCount:
                type: integer
              lastUpdated:
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
