---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: assetpools.kentra.sh
spec:
  group: kentra.sh
  names:
    kind: AssetPool
    listKind: AssetPoolList
    plural: assetpools
    shortNames:
    - ap
    singular: assetpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.groupCount
      name: Groups
      type: integer
    - jsonPath: .status.totalAssets
      name: Assets
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              description:
                type: string
              pool:
                description: This matches the 'pool:' key in your YAML
                items:
                  properties:
                    assets:
                      items:
                        properties:
                          type:
                            type: string
                          value:
                            type: string
                        required:
                        - type
                        - value
                        type: object
                      type: array
                    name:
                      type: string
                  required:
                  - assets
                  - name
                  type: object
                type: array
            type: object
          status:
            properties:
              groupCount:
                description: Total number of groups (AssetPoolItems)
                type: integer
              lastUpdated:
                type: string
              observedGeneration:
                format: int64
                type: integer
              totalAssets:
                description: Total number of individual assets across all groups
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
