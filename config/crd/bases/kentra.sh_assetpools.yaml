---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: assetpools.kentra.sh
spec:
  group: kentra.sh
  names:
    kind: AssetPool
    listKind: AssetPoolList
    plural: assetpools
    shortNames:
    - ap
    singular: assetpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.itemCount
      name: Items
      type: integer
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AssetPool is the Schema for the assetpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AssetPoolSpec defines the desired state of AssetPool
            properties:
              description:
                description: Description is an optional description of the asset pool
                type: string
              groups:
                description: |-
                  Groups is the structured list of asset groups
                  When Groups is specified, Items is ignored
                items:
                  description: |-
                    AssetGroup represents a logical grouping of multiple asset sets
                    For example: a person with their primary and secondary contact info
                  properties:
                    assetSets:
                      description: |-
                        AssetSets contains multiple named sets of assets
                        Each set will generate its own job
                      items:
                        description: NamedAssetSet represents a named collection of
                          assets
                        properties:
                          assets:
                            description: Assets are the individual assets in this
                              set
                            items:
                              description: AssetItem defines a single asset in the
                                pool
                              properties:
                                type:
                                  description: Type specifies the type of asset (e.g.,
                                    username, email, domain, ip)
                                  type: string
                                value:
                                  description: Value is the actual value of the asset
                                  type: string
                              required:
                              - type
                              - value
                              type: object
                            type: array
                          name:
                            description: Name is the identifier for this asset set
                              (e.g., "primary", "work", "personal")
                            type: string
                        required:
                        - assets
                        - name
                        type: object
                      type: array
                    assets:
                      description: |-
                        Assets is the legacy field for backward compatibility
                        When AssetSets is empty, this will be used as a single asset set
                      items:
                        description: AssetItem defines a single asset in the pool
                        properties:
                          type:
                            description: Type specifies the type of asset (e.g., username,
                              email, domain, ip)
                            type: string
                          value:
                            description: Value is the actual value of the asset
                            type: string
                        required:
                        - type
                        - value
                        type: object
                      type: array
                    name:
                      description: Name is a friendly identifier for this group (e.g.,
                        "john-doe")
                      type: string
                  type: object
                type: array
              items:
                description: Items is the flat list of assets (legacy/simple mode)
                items:
                  description: AssetItem defines a single asset in the pool
                  properties:
                    type:
                      description: Type specifies the type of asset (e.g., username,
                        email, domain, ip)
                      type: string
                    value:
                      description: Value is the actual value of the asset
                      type: string
                  required:
                  - type
                  - value
                  type: object
                type: array
            type: object
          status:
            description: AssetPoolStatus defines the observed state of AssetPool
            properties:
              groupCount:
                type: integer
              itemCount:
                type: integer
              lastUpdated:
                type: string
              observedGeneration:
                format: int64
                type: integer
              totalAssetSets:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
