{{- if .Values.crds.install }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: osints.kentra.sh
spec:
  group: kentra.sh
  names:
    kind: Osint
    listKind: OsintList
    plural: osints
    singular: osint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Osint is the Schema for the osints API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OsintSpec defines the desired state of Osint
            properties:
              additionalEnv:
                items:
                  description: EnvVar represents an environment variable
                  properties:
                    name:
                      description: Name of the environment variable
                      type: string
                    value:
                      description: Value of the environment variable
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              args:
                items:
                  type: string
                type: array
              assetPool:
                type: string
              category:
                type: string
              debug:
                type: boolean
              httpProxy:
                type: string
              periodic:
                type: boolean
              schedule:
                type: string
              storagePool:
                type: string
              tool:
                type: string
            required:
            - tool
            type: object
          status:
            description: OsintStatus defines the observed state of Osint
            properties:
              jobName:
                type: string
              lastExecuted:
                type: string
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
